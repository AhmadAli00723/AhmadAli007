You need to import the springboot project from pom.xml as usual.
Import angular project and just use npm install and then npm start.
It can only create employee and gets the list of employees created.
